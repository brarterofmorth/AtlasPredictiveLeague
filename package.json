{
  "name": "atlas-predictive-league",
  "version": "1.0.0",
  "description": "Privacy-preserving prediction market with Fully Homomorphic Encryption",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "SEPOLIA_RPC_URL=\"https://ethereum-sepolia-rpc.publicnode.com\" npx hardhat run scripts/deploy.cjs --network sepolia",
    "verify": "npx hardhat verify --network sepolia",
    "dev": "cd dapp && npm run dev",
    "build": "cd dapp && npm run build"
  },
  "devDependencies": {
    "@fhevm/hardhat-plugin": "0.3.0-0",
    "@fhevm/mock-utils": "0.3.0-0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.26.3"
  },
  "dependencies": {
    "@fhevm/solidity": "^0.9.0",
    "ethers": "^6.13.0"
  }
}
