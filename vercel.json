{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "cd dapp && npm install && npm run build",
  "outputDirectory": "dapp/dist",
  "framework": null
}
